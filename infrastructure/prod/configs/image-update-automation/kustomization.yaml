apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# flux-system: this automation serves the whole repo, not any one app.
namespace: flux-system

resources:
  - image-update-automation.yaml
