apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: repo-fernfiles-worker
spec:
  image: ghcr.io/activescott/fernfiles/worker
  interval: 5h
  secretRef:
    name: github-container-registry-secret
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: policy-fernfiles-worker
spec:
  imageRepositoryRef:
    name: repo-fernfiles-worker
  policy:
    semver:
      range: ">=0.0.0"
