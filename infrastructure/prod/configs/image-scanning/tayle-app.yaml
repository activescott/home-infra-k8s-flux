apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: repo-tayle-app
spec:
  image: ghcr.io/tayle-co/tayle/app
  interval: 5h
  secretRef:
    name: github-container-registry-secret
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: policy-tayle-app
spec:
  imageRepositoryRef:
    name: repo-tayle-app
  policy:
    semver:
      range: ">=0.0.0"
