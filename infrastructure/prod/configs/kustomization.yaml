resources:
  - ../../base/configs/secret-name-reference
  - ../../base/configs/image-scanning-webhook-receiver
  - ../controllers/cert-manager
  # k3s bundles Traefik; customize it via HelmChartConfig instead of a separate HelmRelease
  - traefik-config.yaml
