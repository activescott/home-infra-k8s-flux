apiVersion: v1
kind: Namespace
metadata:
  name: cloudflare
  labels:
    # Cloudflare managed resources (DNS records, email routing) live here rather than in the
    # email-* namespaces: DNS is cross-cutting infrastructure, not mail-specific. Mail records
    # are simply records in this namespace like any other.
    app.kubernetes.io/name: cloudflare
