apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Copied from activescott/browser-chaperone k8s/base at b1168d77, less
# app-ingress.yaml: the approval app has no UI yet, and its only callers are
# the browser service and kubelet.
resources:
  - app-deployment.yaml
  - app-service.yaml
  - browser-deployment.yaml
  - browser-service.yaml
  - db-statefulset.yaml
  - db-service.yaml
