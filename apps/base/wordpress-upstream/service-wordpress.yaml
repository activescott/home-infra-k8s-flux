apiVersion: v1
kind: Service
metadata:
  name: wordpress
  labels:
    app: wordpress
    role: web
spec:
  type: ClusterIP
  selector:
    app: wordpress
    role: web
  ports:
    - name: http
      port: 80
      targetPort: http
