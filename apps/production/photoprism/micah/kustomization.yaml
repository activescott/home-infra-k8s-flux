resources:
  - namespace.yaml
  - ../../../base/photoprism
  - certificate.yaml

secretGenerator:
  - name: mariadb-creds
    envs:
      - .env.secret.mariadb-creds.encrypted

patches:
  - path: patch-mariadb-volume.yaml
  - path: patch-photoprism-statefulset.yaml
  - path: patch-photoprism-ingress.yaml

namespace: photoprism-micah

labels:
  - pairs:
      app.activescott.com/tenant: micah
