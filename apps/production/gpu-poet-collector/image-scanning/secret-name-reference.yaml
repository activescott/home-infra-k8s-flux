# tells kustomize to update the ImageRepository CRD's secretRef/name when the
# referenced Secret resource is renamed by kustomize hashing/prefixing rules.
nameReference:
  - kind: Secret
    fieldSpecs:
      - kind: ImageRepository
        path: spec/secretRef/name
