apiVersion: v1
kind: PersistentVolume
metadata:
  name: coinpoet-prod-indexnow-pv
spec:
  capacity:
    storage: 100Mi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-path
  hostPath:
    path: /mnt/thedatapool/app-data/coinpoet/prod/indexnow
