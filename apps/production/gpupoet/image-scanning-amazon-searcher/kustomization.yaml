apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/image-scanning/timestamp

configurations:
  - secret-name-reference.yaml

patches:
  # Customize ImageRepository
  - target:
      kind: ImageRepository
      name: repo-placeholder
    patch: |-
      - op: replace
        path: /metadata/name
        value: repo-amazon-searcher
      - op: replace
        path: /spec/image
        value: ghcr.io/activescott/amazon-searcher

  # Customize ImagePolicy
  - target:
      kind: ImagePolicy
      name: policy-placeholder
    patch: |-
      - op: replace
        path: /metadata/name
        value: policy-amazon-searcher
      - op: replace
        path: /spec/imageRepositoryRef/name
        value: repo-amazon-searcher

  # Customize ImageUpdateAutomation
  - target:
      kind: ImageUpdateAutomation
      name: update-placeholder
    patch: |-
      - op: replace
        path: /metadata/name
        value: update-amazon-searcher
