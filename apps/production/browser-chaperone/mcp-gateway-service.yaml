apiVersion: v1
kind: Service
metadata:
  name: mcp-gateway
  namespace: browser-chaperone-prod
spec:
  selector:
    app: mcp-gateway
  ports:
    - name: http
      port: 3700
      targetPort: 3700
      protocol: TCP
